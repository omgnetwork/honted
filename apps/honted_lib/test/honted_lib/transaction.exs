defmodule HonteDLib.TransactionTest do
  @moduledoc """
  This test should test the stateless features of the transaction validation etc.
  
  Since, from the point of view of Lib's and API's API, it is only a convenience functionality, let's postpone this
  
  FIXME: implement these tests (:troll:)
  """
end
