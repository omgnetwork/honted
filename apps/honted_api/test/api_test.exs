defmodule HonteD.APITest do
  @moduledoc """
  This test should test only the stateful features of the API

  e.g. that for the create_x_transaction methods the nonce is fetched corectly

  for stateless tests of create_x_transaction functionality see test for HonteD.Transaction

  FIXME: implement the statefull tests using mocks of TendermintRPC
  """
end
