defmodule HonteD.EventsTest do
  @moduledoc """
  Tested via the public API in HonteD.API tests. The tests were moved there (I think they belong there more naturally, as they involve HonteD.API)
  """
end
