defmodule HonteDEventsTest do
  @moduledoc """
  Tested via the public API in HonteDAPI tests. The tests were moved there (I think they belong there more naturally, as they involve HonteDAPI)
  """
end
