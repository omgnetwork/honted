defmodule HonteD.Integration do
  @moduledoc """
  Just tests in this app!
  
  The intention is to have an app that depends on all other apps, which could serve as the place to put 
  integration tests
  """
end
